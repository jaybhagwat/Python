#Inserttion sort Implementation
def InsertionSort(in):
	for i in range(len(in)-1):
		if i==0:
			op[i]=in[i]
			

			
def main():
	input_streamam=eval(input("Enter the List to be sorted")
	InsertionSort(input_stream)
	print(input_stream)
	
if __name__=='__main__':
	main()